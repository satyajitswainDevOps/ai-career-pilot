version: "3.9"

services:
  backend:
    build:
      context: ./backend
    container_name: ai-career-backend
    ports:
      - "8000:8000"
    env_file:
      - ./backend/.env
    volumes:
      - ./backend:/app
    restart: unless-stopped